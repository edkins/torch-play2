version: "0.1"
metadata:
  name: example0
  annotations:
    hello: world
data:
  src: Fashion-MNIST
layers:
  - type: dense
    size: 1000
  - type: relu
  - type: dense
    size: 10
  - type: softmax
training:
  device: cuda
  optimizer:
    type: adam
  loss:
    type: cross_entropy
  epochs: 10
  batch_size: 64
